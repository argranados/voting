HOW TO RUN
En local solo corre en containers
docker compose up --buld -d    -> build para reconstruir la imagen

HOW TO TEST
front: http://localhost:3000/

---------------------------------------------------------------------------------------------
Paso 1 — Base de datos local (Docker)
docker compose up -d
docker ps

Por qué así: ddl-auto=validate hace que Flyway cree tablas y JPA solo valide que coincidan.

V1__init.sql
Dos guiones bajos después de la versión.

http://localhost:8080/actuator/health
./mvnw spring-boot:run

Paso 5 — Convertir tus clases a @Entity (sin relaciones aún)
Paso 6 — Crear los Repositories
2026-03-01T20:54:44.018-06:00  INFO 7864 --- [voting] [  restartedMain] .s.d.r.c.RepositoryConfigurationDelegate : 
Finished Spring Data repository scanning in 38 ms. Found 5 JPA repository interfaces.
Paso 7 — Implementar “Votar” (servicio + endpoint)
Paso 8 — Endpoint público POST /api/v1/public/votes
    POST http://localhost:8080/api/v1/public/votes
Paso 9 — Probar (insertar datos sql)

docker exec -it voting_db psql -U voting -d voting
insert into season (name, status)
values ('Temporada 1', 'ACTIVE')
returning id;

insert into contestant (season_id, name, status)
values
  (1, 'Famoso A', 'ACTIVE'),
  (1, 'Famoso B', 'ACTIVE'),
  (1, 'Famoso C', 'ACTIVE')
returning id, name;

Paso 10 — Endpoint /api/v1/admin/rounds/{id}/results
    GET http://localhost:8080/api/v1/admin/rounds/1/results
Paso 11: cerrar ronda (OPEN → CLOSED) y bloquear votos para completar el flujo de la semana.
    POST http://localhost:8080/api/v1/admin/rounds/1/close
Paso 12 — (Mini mejora) Validaciones de estado (recomendado)

Flujo Real de la Ronda
    create round -> SCHEDULED
    nominate contestants
    open voting -> OPEN
    people vote
    close voting -> CLOSED
    calculate results

Paso 12 — GET /public/rounds/current    

Paso 13 — GET /public/rounds/{id}/nominees

insert into round (
    season_id,
    name,
    starts_at,
    ends_at,
    status,
    rule_type
)
values (
    1,
    'Semana 2',
    now(),
    now() + interval '2 days',
    'SCHEDULED',
    'ELIMINATE_LOWEST'
)
returning id;

Cada vez que crees una ronda nueva, tu checklist manual es:
insert round (SCHEDULED)
insert nominations (round_id + contestant_id) ✅
open round
votar

Paso 14 — Regla: solo 1 ronda OPEN por season
Paso 15 — Crear rondas desde API
http://localhost:8080/api/v1/admin/rounds
Paso 16 — Nominar participantes desde API

Paso 18 — Agregar ResultsServiceTest (como te prometí)
Siguiente paso recomendado (portafolio + entrevistas): 1 test de Controller (MockMvc)

4-marzo
1️⃣ DTOs de respuesta (mejor práctica REST)
2️⃣ Un test HTTP extra para validar el error cuando la ronda está CLOSED.
5-Marzo
Siguiente paso: DTO para Nominees (mejora pequeña pero profesional)
Último paso técnico del MVP: Test HTTP para error cuando la ronda está CLOSED
add test for controller
Paso 1 (siguiente): Dockerizar el proyecto

docker compose down -v
docker compose up --build

docker exec -it voting_db psql -U voting -d voting
psql -U voting -d voting -f dev_seed.sql

6-marzo //Run k6 Test
docker exec -it voting_db psql -U voting -d voting
// insertar datos
voting=# -- Limpiar tablas respetando dependencias
TRUNCATE TABLE vote CASCADE;
TRUNCATE TABLE nomination CASCADE;
TRUNCATE TABLE round CASCADE;
TRUNCATE TABLE contestant CASCADE;
TRUNCATE TABLE season CASCADE;

-- Insertar datos de prueba
INSERT INTO season (id, name, status, created_at)
VALUES (1, 'Season 1', 'ACTIVE', now());

INSERT INTO contestant (id, season_id, name, status, created_at)
VALUES
    (1, 1, 'Contestant A', 'ACTIVE', now()),
    (2, 1, 'Contestant B', 'ACTIVE', now());

-- Validar datos insertados
SELECT * FROM season;
SELECT * FROM contestant;

voting=# \q

//run script
Poncho@DESKTOP-FR57KQ4 MINGW64 /d/Documentos/repo/voting/voting (main)
$ ./script/runTest.sh

7-Marzo
Deploy options
1. EC2 + Docker Compose
2. AWS App Runner
3. ECS con Fargate
4. Elastic Beanstalk
5. EKS (Kubernetes)

haremos las primeras tres,
1. EC2 + Docker Compose:
----------------------------------------------
1️⃣ checkout del repo
2️⃣ configuración de SSH
3️⃣ conexión a EC2
4️⃣ git pull
5️⃣ docker compose up --build -d
6️⃣ deploy del servicio

git push
      ↓
GitHub Actions
      ↓
SSH
      ↓
EC2
      ↓
docker compose up --build
      ↓
Aplicación desplegada
Eso ya es un CI/CD func 

9-Marzo
cd /home/ec2-user/voting/voting
docker ps
docker compose up -d
docker compose up --build -d

ssh -i /root/.ssh/ec2-keypar.pem ec2-user@3.141.244.116

11-Marzo Ejecutar Test k6
docker exec -i voting_db psql -U voting -d voting < script/db/cleanup_test_db.sql
docker exec -i voting_db psql -U voting -d voting < script/db/setup_test_db.sql
$ ./script/runTest.sh

12-marzo
add season and contestant controller
1️⃣ integrar estos endpoints en el frontend
Reconstruir FrontEnd
docker compose up --build -d frontend

front: http://localhost:3000/

ver logs 
$ docker logs -f 349b8dd6bc1b
$ docker logs voting-service

Probar Front
Parece que se estan repitiendo los votos
voting=# select id, round_id, contestant_id, status, created_at from vote where round_id = 6 order by id;
 id | round_id | contestant_id |  status  |          created_at
----+----------+---------------+----------+-------------------------------
 16 |        6 |             1 | ACCEPTED | 2026-03-12 23:26:01.870727+00
 17 |        6 |             1 | ACCEPTED | 2026-03-12 23:26:06.928249+00
 18 |        6 |             3 | ACCEPTED | 2026-03-12 23:26:46.935437+00
 19 |        6 |             3 | ACCEPTED | 2026-03-12 23:26:48.389896+00
 20 |        6 |             5 | ACCEPTED | 2026-03-12 23:26:49.774543+00
 21 |        6 |             5 | ACCEPTED | 2026-03-12 23:26:51.087981+00
 22 |        6 |             1 | ACCEPTED | 2026-03-12 23:26:52.502748+00
 23 |        6 |             1 | ACCEPTED | 2026-03-12 23:26:53.683586+0

 Mejoras:
 1. Mostrar nombre en vez de ID
 2. Deshabilitar botón al votar
 4. Agregar estados de contestant para UX
 5. Mostrar “Last Season”

 16-Abril
 Cambiar EC2 a Elastic IP, recordar quitarla para no generar costos

ssh -i Desktop/keys/voting/voting-key.pem ec2-user@52.55.67.197
docker compose  logs -f voting-service