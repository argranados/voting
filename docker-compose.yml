version: "3.9"
services:
  db:
    image: postgres:16
    container_name: voting_db
    environment:
      POSTGRES_DB: voting
      POSTGRES_USER: voting
      POSTGRES_PASSWORD: voting
    ports:
      - "5432:5432"
    volumes:
      - voting_pg:/var/lib/postgresql/data

volumes:
  voting_pg: